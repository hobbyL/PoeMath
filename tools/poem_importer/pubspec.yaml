name: poem_importer
description: >
  PoeMath data pipeline CLI - transforms curated YAML sources into asset JSON
  files for poems, authors and math formulas.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0
  yaml: ^3.1.2
  lpinyin: ^2.0.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
